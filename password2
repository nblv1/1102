import random
import string


my_pass_len = input('Довжина пароля  ')
codes = list(string.ascii_uppercase + string.ascii_lowercase + string.digits)
password = [random.choice(codes) for x in range(int(my_pass_len))]# генерує пароль з кількістю заданих рядків
print(''.join(password))
